# Deprecated SQL (구 스키마)

아래 파일들은 이전 소문자/마스터 명칭 스키마용입니다.
현재는 `../init_schema.sql` 또는 `scripts/db_cleanup/` 스크립트를 사용하세요.

- init_pg_masters.sql
- init_pg_transactions.sql
- migrate_card_master_columns.sql
